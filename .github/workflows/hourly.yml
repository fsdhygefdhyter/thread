name: Generate Threads Post

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test cron
        run: echo "Cron triggered at $(date)"
